import 'package:flutter/material.dart';

class AppColors {
  // Màu chủ đạo
  static const primary = Color(0xFF1DB954);    // Xanh Spotify
  static const background = Color(0xFF191414); // Nền tối
  static const card = Color(0xFF282828);       // Nền thẻ (card)
  static const accent = Color(0xFFFFFFFF);     // Màu nhấn / chữ trắng

  // Màu chữ
  static const text = Color(0xFFFFFFFF);       // Chữ chính
  static const subtitle = Color(0xFFB3B3B3);   // Chữ phụ (xám Spotify)
}
